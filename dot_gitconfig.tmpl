{{ if eq .org "personal" }}
[init]
    defaultBranch = main
{{ end }}

[commit]
	template = {{ .chezmoi.homeDir }}/.git-template

[core]
	pager = less -FX

[color]
	ui = true

[alias]
	graph = log --graph --pretty=format:'%Cred%h%Creset - %C(cyan)<%an>%Creset%C(yellow)%d%Creset %s %Cgreen(%cr)' --abbrev-commit --date=relative
	who = blame -c

[rerere]
	enabled = true

[fetch]
	prune = true

[push]
	default = current
	followTags = true

[pull]
	rebase = false

[gitflow "prefix"]
	feature = feature/
	bugfix  = bugfix/
	release = release/
	hotfix  = hotfix/
	support = support/
	versiontag =

[gitflow "branch"]
{{ if eq .org "softiron" }}
	master  = master
{{ else }}
	master  = main
{{ end }}
	develop = develop

{{ if eq .org "softiron" }}
[url "ssh://git@git.softiron.com:30022"]
	insteadOf = https://git.softiron.com
{{ end }}

[user]
	name  = {{ .fullname }}
	email = {{ .email }}

